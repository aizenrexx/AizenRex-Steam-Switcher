<Page x:Class="SteamSwitcher.Pages.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="0,0,8,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Logs" Style="{StaticResource PageTitle}" />
            <TextBlock x:Name="SubtitleText" Style="{StaticResource PageSubtitle}"
                       Text="Application log. Rotates automatically at 1 MB." />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:TextBox x:Name="SearchBox" Grid.Column="0"
                         PlaceholderText="Search the log…"
                         TextChanged="OnFilterChanged" Margin="0,0,10,0" />

                <ComboBox x:Name="LevelFilter" Grid.Column="1" Width="120"
                          SelectedIndex="0" SelectionChanged="OnFilterChanged"
                          Margin="0,0,10,0">
                    <ComboBoxItem Content="All levels" />
                    <ComboBoxItem Content="Errors" />
                    <ComboBoxItem Content="Warnings" />
                    <ComboBoxItem Content="Info" />
                    <ComboBoxItem Content="Debug" />
                </ComboBox>

                <Button Grid.Column="2" Style="{StaticResource SubtleButton}"
                        Content="Reload" Click="OnReloadClick" Margin="0,0,8,0" />

                <Button Grid.Column="3" Style="{StaticResource SubtleButton}"
                        Content="Export" Click="OnExportClick" Margin="0,0,8,0" />

                <Button Grid.Column="4" Style="{StaticResource DangerButton}"
                        Content="Clear" Click="OnClearClick" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="CountText" Grid.Row="0" Style="{StaticResource BodyText}"
                           Margin="0,0,0,10" Text="" />

                <ListBox x:Name="LogList" Grid.Row="1"
                         Background="Transparent" BorderThickness="0"
                         FontFamily="Cascadia Mono, Consolas, Courier New"
                         FontSize="11.5"
                         Foreground="{StaticResource TextSecondaryBrush}"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True" />
            </Grid>
        </Border>
    </Grid>
</Page>
