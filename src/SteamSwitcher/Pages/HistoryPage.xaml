<Page x:Class="SteamSwitcher.Pages.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="0,0,8,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="History" Style="{StaticResource PageTitle}" />
            <TextBlock Style="{StaticResource PageSubtitle}"
                       Text="Every switch, repair and restore this app has performed." />
        </StackPanel>

        <!-- Stats -->
        <ItemsControl Grid.Row="1" x:Name="StatsHost" Margin="0,0,0,4">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource MetricCard}" MinWidth="130">
                        <StackPanel>
                            <TextBlock Text="{Binding Label}" Style="{StaticResource LabelText}" />
                            <TextBlock Text="{Binding Value}" FontSize="22" FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ui:TextBox x:Name="SearchBox" Grid.Column="0"
                             PlaceholderText="Search history…"
                             TextChanged="OnFilterChanged" Margin="0,0,10,0" />

                    <ComboBox x:Name="ResultFilter" Grid.Column="1" Width="130"
                              SelectedIndex="0" SelectionChanged="OnFilterChanged"
                              Margin="0,0,10,0">
                        <ComboBoxItem Content="All results" />
                        <ComboBoxItem Content="Success only" />
                        <ComboBoxItem Content="Failed only" />
                    </ComboBox>

                    <Button Grid.Column="2" Style="{StaticResource DangerButton}"
                            Content="Clear history" Click="OnClearClick" />
                </Grid>

                <DataGrid x:Name="HistoryGrid" Grid.Row="1" Style="{StaticResource CleanGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="When" Binding="{Binding TimeText}" Width="150" />
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="170" />
                        <DataGridTextColumn Header="Result" Binding="{Binding StatusText}" Width="80" />
                        <DataGridTextColumn Header="Detail" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
